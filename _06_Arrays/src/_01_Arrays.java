import java.sql.Array;
import java.util.Arrays;

public class _01_Arrays {

    public static void main(String[] args) {

        /*
            Como vimos en la introducción a los Arrays en Java, son estructuras de datos que te permiten almacenar múltiples valores del
            mismo tipo bajo un solo nombre. Normalmente este tipo de datos tienen alguna relación entre si. La declaración es la siguiente:
         */

        int[] numeros = new int[5];

        /*
            int: Tipo del Array.
            []: Indicamos que el Array es de una dimensión.
            numeros: Nombre de la variable donde se va a almacenar el Array.
            [5]: Nº de valores que va a almacenar la matriz.
         */

    }
}